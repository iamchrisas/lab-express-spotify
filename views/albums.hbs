<h1>Albums</h1>
<ul>
  {{#each albums}}
    <li>
      <h3>{{this.name}}</h3>
      <img
        src="{{this.images.0.url}}"
        alt="{{this.name}}"
        style="width:200px; height:auto;"
      />
      <br />
      <a href="/tracks/{{this.id}}">View Tracks</a>
    </li>
  {{/each}}
</ul>